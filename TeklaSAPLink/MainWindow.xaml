<Window x:Class="TeklaSAPLink.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TeklaSAPLink"
        mc:Ignorable="d"
        Title="TeklaSAPLink_v0.1" Height="228.671" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="86*"/>
            <ColumnDefinition Width="203*"/>
        </Grid.ColumnDefinitions>
        <TextBox Name="textBox7" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Visible" IsReadOnly="True" BorderThickness="3" ClipToBounds="True" TextWrapping="WrapWithOverflow" IsEnabled="True" Grid.Column="1" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="14*"/>
                <RowDefinition Height="85*"/>
            </Grid.RowDefinitions>
            <Button Content="Проверка изменений" Click="Button_Click"/>
        </Grid>
    </Grid>
</Window>
